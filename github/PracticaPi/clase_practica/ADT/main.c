#include <stdio.h>
#include <assert.h>

int main(){
    //funcion sin param, devuelve punteros a double
    double *(*p)();

    //funcion que recibe puntero a char, devuele matriz 3x5
    int (*p(char *c))[3][5]; 

    //p es un o
}
